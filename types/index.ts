export interface Video {
    id: string
    title: string
    description: string
    publicId: string
    orignalSize: number
    compressedSize: number
    duration: number
    createdAt: Date
    updatedAt: Date
}